关于我的一个个人博客的chat界面，其前端在BlogFrontEnd\src\views\Chat.vue定义。
3）修改对话后，不是把后面的全删了，而是保留分支。
4）分支应该可以可视化。
5）对话备份
6）记忆管理，性格变化，礼物系统，衣柜系统，重大事件，角色建模（也就是建模user形象）
7）ios背景！至少可以确定，文章列表页可以换为fixed背景
10）底部正式的底边栏
11）ios顶部的白色可以看作云朵，可以做一个下雪动画。
15）RAG
16）日志


要有搞科研的心，而不是混论文的心


solved：
1）对话气泡貌似会超出整体界面的宽度，从而不显示，怎么解决这一问题？我现在测试了：
wwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwwww
这在会话中只会显示为一行，并不会换行。而如果是同长度的汉字，就没这个问题。

2）经过我的实机测试，chat页面在移动端（不论是Android还是IOS）上最外层的页面是可以上下滑动的，这导致体验很不好。
一个合适的对话网页应当是只有消息列表、历史对话列表可以滑动，其余部分保持固定。

8）导航栏试着搞个渐变色
5）左侧栏歪了

6）对话页面按下“/”即可定位到对话框

12）这是一个个人博客项目，其中有一个页面BlogFrontEnd\src\views\Chat.vue的功能为和AI对话。我发现了一个很严重的问题——不同api供应商的接口是不同的，而我现在只是笼统地给了一套固定参数、固定流程，这很不方便后续接口修改，而且会存在参数冗余甚至是错误。
（比如Deepseek的是否联网参数当前的是错误的，正确的应该是另一个；以及，Deepseek的推荐对话场景下temperature为1.3，而其他的api如grok则可能不同；Deepseek提供了函数调用功能...我可以列出来一堆特性，希望你能明白我在说什么）
所以，我希望你能按供应商，把不同供应商对应的代码分类到对应的目录中，这方便我后续单独配置。而那些通用的函数则抽离到通用的文件中，类似当前的情况。
这是一个很艰难的任务，我希望先做足仔细、深刻的规划，然后再着手修改代码。
为了保证配置的不冗余，我当前的设计思路是除了各别放到前端更合适的参数（如preset的默认头像），尽量把配置放到后端，尽量减少重复配置的数据，尽量把配置从代码中抽离出来，防止硬编码。
请开始这项无比艰巨的任务吧

13）不要出现代码结构混乱的问题，现在BlogFrontEnd\src\config\chat.js和BlogFrontEnd\src\views\chat\constants.js都负责chat页面的常量存储。这是正常的吗？另外，Chat相关的其他代码地方是否存在这种结构分工混乱的问题？我看到BlogFrontEnd\src\views\chat下有很多js文件，而BlogFrontEnd\src\components\Chat里的vue文件中也有很多js文件，这种分离的不干净的感觉是否会导致混乱？还是说现在已经足够清晰了，这些vue文件中的js代码是有必要保留到vue文件中的？
总结为一句话，我认为要确保整个项目的可维护性和清晰程度。请你帮我检查是否存在上述的问题，如有必要，请做出调整。

9）对话完后会往上跳一段

3）移动端，菜单按钮点击后，不是显示一个黑色遮罩层，而是一个小一点的，更美观的列表

2）前端对ai输出的内容的markdown支持不够，比如:
```python
a=1
```
这种就没有渲染成代码块。请优化这一点

14）登录界面回车登录，另外跳转到目标界面，而不是默认admin